#/usr/bin/python3

#serve files in current directory on port 8000.

python3 -m http.server

# You can add an alias to .bashrc to access this feature more easily. echo alias serve='python3 -m http.server' >> ~/.bashrc && source ~/.bashrc
